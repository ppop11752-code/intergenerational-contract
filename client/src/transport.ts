import type{PlayerSnapshot,RoomSnapshot}from"./types.js";
declare const io:(url?:string,opts?:any)=>any;
export class GameTransport{socket:any;room:RoomSnapshot|null=null;player:PlayerSnapshot|null=null;onChange=()=>{};constructor(){this.socket=io(undefined,{transports:["websocket","polling"]});this.socket.on("room:state",(x:RoomSnapshot)=>{this.room=x;this.onChange()});this.socket.on("player:state",(x:PlayerSnapshot)=>{this.player=x;this.onChange()})}emit<T=any>(event:string,payload:any={}):Promise<T>{return new Promise(r=>this.socket.emit(event,payload,(x:T)=>r(x)))}createRoom(playerId:string,displayName:string){return this.emit<any>("room:create",{playerId,displayName})}joinRoom(code:string,playerId:string,displayName:string){return this.emit<any>("room:join",{code,playerId,displayName})}start(){return this.emit<any>("game:start",{})}replay(){return this.emit<any>("game:replay",{})}action(action:any){return this.emit<any>("game:action",{action})}}
