handoff_id: H-20260906-012-06-OI004-QA-DEFECTS
from: 07
to: 06
status: OPEN
title: Fix OI-004 Tutorial QA defects before E2E rerun

## Context

Chat 07 static QA found two client implementation defects against `docs/UI_TUTORIAL_SPEC.md`.

## Defects

1. Help recap is opened with `HTMLDialogElement.showModal()` in `client/src/main.ts`, but the Tutorial spec requires the help recap to be non-modal and non-blocking.

2. Birth/T7 availability is gated only by `player.financial.representative` in both Tutorial unlock logic and Birth panel button. The spec requires T7 to appear only when Birth is actually available from authoritative gameplay state and says to omit/defer when required authoritative data is unavailable rather than guessing.

## Source

- `docs/UI_TUTORIAL_SPEC.md` sections 4/T7, 7, 9, 11
- `client/src/main.ts`
- `client/src/tutorial.ts`
- `client/src/types.ts`
- `server/backend/src/authoritative-room.ts`
- `reports/07_CURRENT.md`

## Required work

1. Make Tutorial help recap non-modal/non-blocking.
2. Add authoritative Birth eligibility exposure/consumption only as needed by existing rules; do not invent gameplay eligibility.
3. Gate Birth UI and T7 from authoritative eligibility, or defer T7 when the server snapshot does not provide enough data.
4. Add/update regression tests covering both defects.
5. Run client build/tests.
6. Hand back to Chat 07 after fixes.

## Constraints

- Do not change gameplay rules or constants.
- If server snapshot shape must expose an existing eligibility fact, coordinate with Chat 03 rather than redefining the rule client-side.

## Expected output

- Both defects fixed and tested.
- Exact commit/ref and test evidence.
- Handoff back to Chat 07 for integration/E2E rerun.

## Result

Pending Chat 06.
