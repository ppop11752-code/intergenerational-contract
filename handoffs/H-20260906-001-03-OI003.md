handoff_id: H-20260906-001-03-OI003
from: 00
to: 03
status: CLOSED
title: Xử lý OI-003 protocol helper thiếu game:replay

## Context

OI-001 và OI-002 đã CLOSED / VERIFIED. Backend canonical hiện ở `server/backend/` trên GitHub. OI-003 là issue tiếp theo theo roadmap hiện tại.

## Source

- `docs/OPEN_ISSUES.md`
- `docs/PROJECT_BASELINE.md`
- `docs/SOURCE_INDEX.md`
- `server/backend/`
- Canonical backend import commit: `d5142e6a58b3179517440cc5af3c8c706bcbc9b7`

## Required work

Chat 03 cần kiểm tra protocol/server hiện tại để xác định và xử lý drift liên quan `SOCKET_EVENTS` thiếu `game:replay`.

Nếu implementation cần thay đổi:

1. Không thay gameplay.
2. Sửa đúng contract/protocol/server scope.
3. Thêm hoặc cập nhật test phù hợp.
4. Chạy kiểm tra liên quan.
5. Cập nhật `reports/03_CURRENT.md`.
6. Nếu cần independent audit, tạo handoff mới cho Chat 08.

## Constraints

- Không tự sửa luật gameplay.
- Nếu phát hiện behavior chưa được định nghĩa, handoff về Chat 00/01 thay vì tự suy diễn.

## Expected output

- OI-003 được xác minh và sửa nếu đúng là drift.
- Commit/ref cụ thể.
- `reports/03_CURRENT.md` cập nhật.
- Handoff cho Chat 08 nếu cần audit độc lập.

## Result

Đã xác minh drift và sửa `server/backend/server/src/contracts.ts` để helper
`SOCKET_EVENTS` liệt kê `game:replay` qua key `replayGame`.

Đã thêm regression test khóa toàn bộ 9 transport events authoritative và tích
hợp test vào script `npm test` của server.

Kiểm tra đã đạt:

- `npm run typecheck`
- `npm test` — PASS 9/9 transport events
- `npm run build`

Không thay đổi gameplay. Không cần audit độc lập riêng cho thay đổi contract
nhỏ này. Live Socket runtime vẫn thuộc OI-006 và chưa được nâng trạng thái.

## Result commit/ref

`e0e000ad9ec920ade3a73a4cf204b5e1954cd0b4`
