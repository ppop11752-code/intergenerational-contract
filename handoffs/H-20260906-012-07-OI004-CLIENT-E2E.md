handoff_id: H-20260906-012-07-OI004-CLIENT-E2E
from: 04
to: 07
status: OPEN
title: Run OI-004 browser/server E2E on canonical Render client

## Context

Chat 04 completed canonical same-origin client deployment for handoff `H-20260906-011-04-CLIENT-STATIC-DEPLOY`.

The existing Render service now builds both backend and canonical `client/`, sets `STATIC_DIR=/app/client`, serves `client/index.html` + CSS + compiled `dist/`, and keeps same-origin Socket.IO via `io(undefined, ...)`.

Implementation commit:

`79202809a8bfaf8a1ffd5667f6bf7c4ca44e16da`

Render deploy:

`dep-daen607avr4c73bbin2g`

Live URL:

`https://intergenerational-contract.onrender.com`

## Verified by Chat 04

- Server build passed.
- Client TypeScript build passed.
- Client static files were copied into runtime image.
- Backend started successfully.
- Render deployment reached LIVE.
- Client and Socket.IO use the same origin by construction.

## Required work

1. Open the live URL in a real browser-capable test path.
2. Verify page renders with no missing static assets/module errors.
3. Verify Socket.IO connection establishes successfully.
4. Verify room create/join path against the authoritative backend.
5. Continue the OI-004 tutorial/browser E2E checks from prior QA handoff.
6. If clean, update `reports/07_CURRENT.md` and route closure/status to Chat 00 as appropriate.
7. If a client implementation defect remains, hand off Chat 06; if a server protocol/runtime defect remains, hand off Chat 03; if deployment/static-serving defect remains, hand back Chat 04.

## Constraints

- Do not change gameplay rules.
- Preserve authoritative backend protocol.

## Expected output

- Browser-level page-load result.
- Socket.IO connection result.
- OI-004 browser/server E2E result.
