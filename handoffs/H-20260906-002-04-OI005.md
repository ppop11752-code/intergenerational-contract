handoff_id: H-20260906-002-04-OI005
from: 00
to: 04
status: CLOSED
title: Re-verify OI-005 Render/GitHub deployment tree mismatch

## Context

OI-001, OI-002 và OI-003 đã CLOSED / VERIFIED. Backend canonical hiện ở `server/backend/` trên GitHub. OI-005 yêu cầu xác minh lại deployment tree trước release-gate OI-006.

## Source

- `docs/OPEN_ISSUES.md`
- `docs/PROJECT_BASELINE.md`
- `server/backend/Dockerfile`
- `server/backend/server/package.json`
- `server/backend/server/src/`
- `reports/04_CURRENT.md`

## Required work

Re-verify OI-005: đối chiếu cấu trúc deploy/Render với canonical GitHub tree, không thay gameplay.

## Result

CLOSED ở cấp source-tree/config-path verification.

Lỗi gốc trong Migration Pack/first Render deploy là Docker `COPY server/src ./server/src` không tìm thấy source vì GitHub tree khi đó thiếu nested server source.

Cây canonical hiện tại đã có đầy đủ:

- `server/backend/server/src/index.ts`
- `server/backend/server/src/game-room.ts`
- `server/backend/server/src/contracts.ts`

Dockerfile hiện nằm tại `server/backend/Dockerfile` và các `COPY package*.json`, `COPY src ./src`, `COPY server/...` chỉ nhất quán khi Docker build context / Render Root Directory là `server/backend/`.

Server runtime target cũng nhất quán: Dockerfile CMD và `server/backend/server/package.json` đều chạy `node dist/server/src/index.js`.

Do đó mismatch GitHub tree làm fail `COPY server/src` đã được loại bỏ. Không cần sửa gameplay/server logic.

Phần chưa được chứng minh là Render service hiện tại có thực sự dùng Root Directory `server/backend/`, fresh cloud build có thành công, `/health` có phản hồi và Socket runtime có hoạt động với dependency thật hay không. Phần này thuộc OI-006.

## Result commit/ref

`cec44a3845471480d40d4d98beb4daf56c31a803` — cập nhật `reports/04_CURRENT.md` với bằng chứng OI-005.
