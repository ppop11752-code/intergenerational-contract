handoff_id: H-20260906-003-04-OI006
from: 04
to: 04
status: OPEN
title: Verify dependency-backed live server runtime

## Context

OI-005 source-tree mismatch is closed. Canonical backend deploy root is `server/backend/`; nested `server/src` now exists and Dockerfile COPY paths are internally consistent for that build context.

OI-006 remains the release gate because actual dependency-backed cloud runtime has not yet been verified.

## Source

- `reports/04_CURRENT.md`
- `server/backend/Dockerfile`
- `server/backend/server/package.json`
- `server/backend/.env.example`
- `docs/OPEN_ISSUES.md`
- Closed handoff `H-20260906-002-04-OI005`

## Required work

1. Verify the live Render service is connected to `ppop11752-code/intergenerational-contract` branch `main`.
2. Verify Render Root Directory / Docker build context is `server/backend/` and Dockerfile resolves correctly.
3. Trigger or inspect a fresh deployment from the current canonical commit.
4. Verify dependency install and TypeScript server build complete successfully.
5. Verify process starts with `node dist/server/src/index.js`.
6. Verify `/health` succeeds on the live service.
7. Verify required environment configuration without exposing secrets.
8. Run or coordinate a Socket.IO smoke test against the live service.
9. Do not change gameplay to make deployment pass.
10. Update `reports/04_CURRENT.md`; hand off to Chat 03/07 when live runtime verification is complete.

## Expected output

- Exact deployed commit/ref.
- Build/deploy result.
- `/health` result.
- Socket smoke result or explicit handoff if owned by Chat 03/07.
- Any remaining operational blockers.

## Result

Chưa có.

## Result commit/ref

Chưa có.
