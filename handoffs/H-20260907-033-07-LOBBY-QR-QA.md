handoff_id: H-20260907-033-07-LOBBY-QR-QA
from: 06
to: 07
status: BLOCKED
title: QA Lobby QR deep-link and fallback

## Context

Chat 06 completed `H-20260907-032-06-LOBBY-QR-INTEGRATION` against locked `docs/UI_QR_CONTRACT_V1.md`.

## Required QA

1. Lobby shows large room PIN and a scannable QR simultaneously.
2. Decoded QR payload is exactly same-origin `/?room=<UPPERCASE_ROOM_CODE>` and contains no reconnect token/playerId/display name/private state.
3. Opening a valid deep-link starts on normal Landing; after entering Join flow, the room field is prefilled and helper text appears.
4. No automatic `room:join` occurs before the user explicitly presses `THAM GIA`.
5. Invalid `room` query is ignored and normal Join remains usable.
6. Valid-looking stale room uses existing server Join error unchanged.
7. Block/fail QRCode.js and verify `Không tạo được mã QR — hãy nhập mã phòng.` while large PIN and Host Start remain usable.
8. `SAO CHÉP LIÊN KẾT` uses the same deep-link payload; clipboard failure is non-blocking.
9. QR modules remain high contrast with clear quiet zone and are not covered by fantasy decoration.
10. Run clean client build/tests including `qr-contract.test.mjs`.

## QA Result

**BLOCKED / FAIL** pending client presentation fix `H-20260907-034-06-LOBBY-QR-CROPPING`.

Verified:
- clean client suite PASS 32/32, including QR contract tests;
- Lobby room PIN is visible;
- QRCode.js reaches ready state and renders a 192×192 QR module image.

Browser defect:
- `.qr-functional` visible box is about 202×202 while the 192×192 QR is rendered inside it with padding;
- artifact screenshot shows the QR clipped on the right and bottom edges;
- actual rendered screenshot cannot be decoded by `jsQR`;
- adding a diagnostic 32px white border after capture still cannot decode it, so the failure is not only missing outer quiet-zone whitespace: modules are already clipped.

Evidence:
- workflow `Lobby QR E2E`
- diagnostic run `34051451933`
- head `196d8e3213b224c538d7a30fc84f48c1d2230e07`
- artifact `9994665230`
- digest `sha256:97a142c12ba2b8a4445d3483dc174d91357077c3b30c6351bdeaff9a712ef03c`
- artifact includes `qr-functional.png` and `results.json`.

The two earlier failures were runner-only and explicitly excluded from product defect classification.

## Handoff

Chat 06 fixes `H-20260907-034-06-LOBBY-QR-CROPPING`, then reopens/returns H033 to Chat 07 for full rerun of decode, deep-link, no-auto-join, stale/invalid query, fallback, copy-link and Host Start checks.

## Constraints
- Do not change gameplay/protocol while testing.
- QR is presentation/navigation only.
- Art-complete remains a separate blocker.
