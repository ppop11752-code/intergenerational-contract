handoff_id: H-20260907-033-07-LOBBY-QR-QA
from: 06
to: 07
status: OPEN
title: QA Lobby QR deep-link and fallback

## Context

Chat 06 completed `H-20260907-032-06-LOBBY-QR-INTEGRATION` against locked `docs/UI_QR_CONTRACT_V1.md`.

Chat 07 first found QR cropping; Chat 06 fixed it in `H-20260907-034-06-LOBBY-QR-CROPPING`. Rerun confirmed desktop/mobile native decode, payload, privacy, deep-link, copy-link, Host Start and manual Join semantics all PASS.

Chat 07 then found renderer-unavailable fallback looping through MutationObserver. Chat 06 fixed that in `H-20260907-035-06-LOBBY-QR-FALLBACK-LOOP`.

## Verified before H035

Main browser QA run `34051902501`, head `46eca73a2363aa77ea0cb2958d277e8c7bf7e1b8`, artifact `9994809830`, digest `sha256:a5fadb67afd962b9445e6ef1482b234418e0bb709205b0b64ba6d89941c29290`.

Clean client suite: **33/33 PASS**.

21 browser production checks PASS before the renderer-unavailable branch:
- large PIN + QR simultaneous;
- desktop QR native decode PASS;
- exact same-origin `/?room=ABC123` payload PASS;
- privacy boundary PASS;
- 192x192 modules + 16px native white quiet zone, no clipping;
- Host Start usable;
- copy-link exact same payload;
- clipboard failure non-blocking;
- compact/mobile native decode PASS;
- valid deep-link -> normal Landing -> Join prefilled uppercase + helper;
- no automatic join before explicit Join;
- explicit Join sends normal request;
- invalid query ignored;
- stale valid-looking code surfaces existing `ROOM_NOT_FOUND` unchanged.

## H035 fix to verify

`client/src/qr-runtime.ts` now makes same-host/same-URL QR rendering idempotent for both:
- `data-qr-ready="1"` successful QR;
- `data-qr-ready="0"` graceful fallback.

When QRCode.js is unavailable, fallback is rendered once and subsequent MutationObserver refreshes must not rewrite the same host. Required text remains:
`Không tạo được mã QR — hãy nhập mã phòng.`

A newly created Lobby host with no settled data attribute may attempt rendering again normally; there are no retry timers.

## Required rerun

1. Run clean client build/tests.
2. Re-run renderer-unavailable harness.
3. Confirm fallback renders once/stabilizes and page does not hang.
4. Confirm exact fallback text.
5. Confirm large PIN and Host Start remain usable.
6. Confirm copy-link/manual deep-link remains usable in fallback state.
7. Reconfirm no gameplay/server/protocol change.

## Constraints
- QR is presentation/navigation only.
- Do not change gameplay/protocol while testing.
- Art-complete remains a separate blocker.
