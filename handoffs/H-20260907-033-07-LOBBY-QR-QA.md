handoff_id: H-20260907-033-07-LOBBY-QR-QA
from: 06
to: 07
status: OPEN
title: QA Lobby QR deep-link and fallback

## Context

Chat 06 completed `H-20260907-032-06-LOBBY-QR-INTEGRATION` against locked `docs/UI_QR_CONTRACT_V1.md`.

Chat 07 previously found a browser presentation defect: the 192x192 QR was clipped by legacy `.qr-placeholder` fixed sizing. Chat 06 fixed it in `H-20260907-034-06-LOBBY-QR-CROPPING`.

## Required QA rerun

1. Lobby shows large room PIN and a scannable QR simultaneously.
2. Actual rendered QR screenshot decodes successfully.
3. Decoded payload is exactly same-origin `/?room=<UPPERCASE_ROOM_CODE>` and contains no reconnect token/playerId/display name/private state.
4. Verify full 192x192 modules plus clear quiet zone on all four sides; no right/bottom clipping on desktop or compact/mobile layout.
5. Opening a valid deep-link starts on normal Landing; after entering Join flow, room field is prefilled and helper text appears.
6. No automatic `room:join` occurs before explicit `THAM GIA`.
7. Invalid `room` query is ignored and normal Join remains usable.
8. Valid-looking stale room uses existing server Join error unchanged.
9. Block/fail QRCode.js and verify plain-language fallback while large PIN and Host Start remain usable.
10. `SAO CHÉP LIÊN KẾT` uses the same payload; clipboard failure is non-blocking.
11. Run clean client build/tests including updated QR layout regression.

## Fix details

`client/src/qr-runtime.ts` now overrides the legacy 112x112 / mobile 88x88 placeholder constraints:
- host width/height auto with min 232x232 and visible overflow;
- functional QR content exactly 192x192;
- 16px white quiet zone via content-box padding;
- QR canvas/image exact 192x192 with no max-width shrinking.

## QA Result

Pending rerun by Chat 07.

## Constraints
- Do not change gameplay/protocol while testing.
- QR is presentation/navigation only.
- Art-complete remains a separate blocker.
