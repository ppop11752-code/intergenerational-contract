import {DEFAULT_CONFIG,GameConfig} from "./config.js"; export function socialContribution(netIncome:number,cfg:GameConfig=DEFAULT_CONFIG){const total=Math.max(0,netIncome)*cfg.socialSecurity.workerContributionRate;return{personal:total*.5,payg:total*.3,support:total*.2,total}} export function pension(personalBalance:number,paygPool:number,elderlyCount:number){return personalBalance/2+(elderlyCount?paygPool/elderlyCount:0)}